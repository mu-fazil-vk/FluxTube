      - name: Build Targeted APK
        run: |
          flutter clean
          flutter pub get
          # Ini perintah buat masak khusus menu arm64-v8a aja, biar langsing!
          flutter build apk --debug --target-platform android-arm64 --split-per-abi
